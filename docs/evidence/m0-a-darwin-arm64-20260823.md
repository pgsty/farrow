# M0-A quick vertical slice — macOS arm64 — 2026-08-23

Result class: **native real E2E passed on the current host only**. This is not
Linux evidence and does not satisfy the complete M0 exit gate by itself.

## Host and inputs

- Host: macOS 26.5.2 (25F84), arm64, invoking UID 501
- QEMU/qemu-img: Homebrew 11.1.0 at `/opt/homebrew/bin`
- Accelerator/machine/CPU: HVF, `virt`, `host`
- Firmware code: `/opt/homebrew/share/qemu/edk2-aarch64-code.fd`
- Firmware vars template: `/opt/homebrew/share/qemu/edk2-arm-vars.fd`
- Image: Canonical Ubuntu Minimal 24.04 release 20260801, arm64 UEFI qcow2
- Image URL: `https://cloud-images.ubuntu.com/minimal/releases/noble/release/ubuntu-24.04-minimal-cloudimg-arm64.img`
- SHA-256: `3a42e0355636bcc4820af28f5bd2c9591502613ab238ad4fa6d4c3659c03d9cf`
- Cached base mode: `0444`; qemu-img virtual size 3.5 GiB, no backing,
  `qemu-img check` clean

## Commands

Native checks before the E2E:

```bash
go test ./...
go test -race ./...
go vet ./...
staticcheck ./...
go test -v ./internal/disk ./internal/qmp
```

Ten-cycle driver:

```bash
./tests/e2e/quick-smoke.sh \
  /Users/vonng/Library/Caches/piglet/images/ubuntu-24.04-minimal-cloudimg-arm64.img \
  3a42e0355636bcc4820af28f5bd2c9591502613ab238ad4fa6d4c3659c03d9cf \
  /Users/vonng/Library/Caches/piglet/evidence/quick-10x-20260823-1839 \
  10
```

## Results

- Start: 2026-08-23T10:38:28Z
- Finish: 2026-08-23T10:44:53Z
- Fresh create/boot/stop/start/stop cycles: 10/10 passed
- QEMU process identities: 20 starts, all UID 501, executable and start time
  captured, expected argv hash stable within each cycle
- QMP: greeting, capabilities, name/UUID identity, powerdown, and quit fallback
  path implemented; normal guest powerdown succeeded
- Guest: native `aarch64`, SSH login user `dba`, generation/spec-hash marker matched
- Root: 64 GiB overlay; observed filesystem size about 60.95 GiB after grow
- Data: sparse 64 GiB qcow2, by-id resolved, ext4, fstab filesystem UUID plus
  `nofail`, persistence canary survived stop/start
- Network: user NAT, DNS resolved, outbound TCP returned HTTP 200, SSH and four
  loopback forwards were accepted by QEMU
- Time: `NTPSynchronized=yes`
- Post-run leak audit: no QEMU process, listening forward, QMP/pid artifact, or
  `/tmp/piglet-m0-*` directory remained
- Post-run disk audit: all 20 generated root/data qcow2 files passed read-only
  `qemu-img check`

Full per-cycle evidence is preserved at:

```text
/Users/vonng/Library/Caches/piglet/evidence/quick-10x-20260823-1839/cycle-*/artifacts/evidence.json
```

The preserved evidence/artifacts consume about 1.2 GiB. They were not deleted.

## Earlier spike and correction

An initial successful lifecycle left only an empty owned mode-0700 runtime
directory. After verifying it was empty and belonged to the test, it was
removed. The harness was then changed to remove its exact owned runtime
directory only after final QMP-verified stop. The formal ten-cycle run reports
`runtime-residue=none` in every cycle.

The first ten-cycle driver invocation also failed before QEMU because its log
redirections made the required-empty work directory non-empty. That failed
driver attempt is preserved at `quick-10x-20260823-1838`; the driver now uses a
dedicated `artifacts/` child and the subsequent formal run passed 10/10.

