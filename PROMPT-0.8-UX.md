# Farrow 0.8 使用体验审计与改进任务

你负责 Farrow 0.8 的使用体验审计和改进。请从用户实际操作出发，找出可以消除的失败、等待、重复操作和理解成本，形成有证据、可执行的任务，并直接完成边界明确的小范围修复。

目标：用户能够顺畅地安装、启动、连接、使用和清理实验环境；常见故障由 Farrow 自行恢复；确实需要人工处理时，用户能看懂发生了什么，并直接执行下一步。

体验对标用户提到的“mini pc”项目。其准确仓库或文档链接尚待确认：如果获得链接，阅读官方资料，必要时在隔离环境体验相同路径；没有链接时，继续按下述标准推进，不猜测项目身份，也不声称已经完成对比。借鉴的是少干预、反馈明确和恢复自然的体验，不要求照搬功能或架构。

## 1. 工作范围与已有成果

代码仓库：`/Users/vonng/pgsty/farrow`。
文档仓库：`/Users/vonng/pgsty/farrow.pgsty.com`。

先检查两个仓库的工作区、当前源码、命令帮助、CHANGELOG、CONTRIBUTING 和 UX-PLAN。历史待办只是线索，要核实哪些仍然存在。保留已有未提交修改，不覆盖其他工作。

Farrow 工作区已经包含以下修复，先验证并接续，不重复当成新发现：

- macOS vmnet 日志目录的安全权限修复，以及保留网络归属证据、避免误报自身网段冲突。
- `start/restart/reload` 接入交互式宿主准备和恢复流程。
- 未缓存镜像的信息查询不依赖 QEMU；本地镜像错误保留真实原因和完整性错误类别。
- 从完好的原私钥恢复缺失公钥；私钥丢失时提供恢复指引。
- 网络后端下载的有限重试、取消和临时文件清理。

这份清单描述工作区成果，不代表已经提交、发布或安装到用户的 Homebrew 路径。

## 2. 判断每一个失败是否真的需要交给用户

对每个阻断点依次回答：

1. 这个步骤是否必须在当前操作中成功？查询信息是否错误地要求运行依赖？辅助功能是否阻断了可用的 VM？
2. 资源是否确实由 Farrow 管理，归属、身份和期望状态能否确认？
3. 能否保留用户意图和已有数据，用明确、有限、可重复执行的动作恢复？
4. 能恢复就执行恢复、重新验证并继续原命令；恢复无效就停止该恢复尝试并保留真实原因，不无限重试。
5. 可选功能无法恢复时，继续独立工作，明确记录哪些功能不可用；必要能力无法恢复时，才返回影响范围准确的错误。

“由我们管理”不等于可以任意重建。优先恢复派生文件、已验证的权限、缓存、运行目录、服务和自动分配的端口。不要用递归 chmod/chown、关闭校验、忽略错误、删除全部状态或重建所有 VM 来掩盖问题。

保留已有 VM 身份、健康磁盘和成功节点。沿用仓库现有实验数据盘恢复策略：只有证实符合重置条件时才处理，并明确报告数据丢弃；设备缺失、探测失败、忙碌挂载和宿主 I/O 故障不等于文件系统损坏。该策略不能扩大到根磁盘、宿主共享目录或无关资源。

真实的 VPN/其他 VM 网段冲突、未知归属、身份不符和摘要不符应保留明确错误。使用已有特权 helper 和网络计划；系统确实需要管理员认证时解释具体用途，并尽量在一次操作中复用认证。已有授权范围内的普通修复直接推进。

## 3. 按用户路径寻找任务

不要只搜索 error 字符串。跟踪命令入口、前置检查、状态变更、恢复分支和最终输出，覆盖下列路径：

### 安装与首次启动

- 干净环境、已有 QEMU/Homebrew、PATH 中多个版本、CLI/helper 不匹配、依赖或固件缺失、下载中断、代理环境。
- 无配置直接 `up`、显式指定配置、从已有部署状态启动。
- 用户是否被迫在 `up → status → setup → up` 之间循环？本可自动完成的准备是否要求用户理解内部组件？
- 交互式终端与脚本分别验证：脚本不能等待隐藏提示；无法继续时给出保留原参数的完整命令。

### 日常使用与中断恢复

- 重复 `up`，`start/stop/restart/reload`，指定部分节点，`--no-wait`，Ctrl-C 后重试，创建到一半，宿主重启，服务意外退出，自动 SSH 端口被占用。
- 恢复后继续原来的命令、节点选择和配置来源，保留 `-f`、仓库选择等参数；保持 `up` 与 `start` 的既有语义。
- 一台节点或一个可选阶段失败，不应无故取消其他独立工作；已有运行节点不能被误报为“本次已验证就绪”。

### 网络、镜像与缓存

- 区分自身网络故障、真实外部冲突、权限不足、探测工具缺失和归属未知；不要让一个权限错误衍生出多条虚假冲突。
- 检查残留 socket、运行目录、服务状态与部分安装现场的恢复。
- 区分 DNS 解析、路由、Guest 出网、镜像仓库可用性与管理 SSH；普通 Guest 就绪不能依赖某个公网网站。
- 检查下载重试、断点续传、缓存校验、损坏缓存恢复和取消清理；不得替换仍被 VM 引用的镜像，不得静默更换用户指定的版本或私有仓库。
- 元数据查询、帮助和离线规划只要求其实际需要的依赖。

### 共享目录与 Guest 功能

- 优先复现宿主共享目录缺失导致整批启动失败的问题：按节点和挂载隔离影响，支持恢复后重试；不能通过创建空目录掩盖路径错误。
- 检查既有项目树的 UID/GID、嵌套文件写入、只读回退及恢复提示；禁止递归修改宿主项目的归属来“修好”共享。
- 复现宿主首选 DNS 故障、备用 DNS 可用而 Guest 解析失败的场景。评估明确 DNS 配置与经验证的回退，保留企业内网和 VPN 的解析语义。
- 评估旧控制节点 SSH 密钥缺失的原地恢复：确认 VM 身份和原部署密钥后修复，避免让用户为派生配置问题重建 VM。
- 数据盘、共享、主机名、节点间 SSH 和私网设置的可选失败，必须准确显示限制，不能把警告吞掉或把未完成挂载标成成功。

### 诊断、清理、升级与性能

- 检查 `doctor/status/logs` 是否能解释当前事实；首次 setup 失败、尚无部署状态时也应有可用诊断。
- 一次 `up → 自动 setup → 重试` 应能用同一操作编号追踪；复用现有日志机制，限制日志体积并避免记录密钥和认证信息。
- 检查 `destroy`、删除持久盘、`purge` 和卸载的最终结果。只按最终事实汇总保留和删除的资源，消除“先说保留、后说删除”的矛盾。
- 验证 0.7 部署升级、旧状态兼容、CLI/helper 配对以及文档和实际安装行为的一致性。
- 分别测量下载、首次启动、已有磁盘启动、健康重复 `up`。复核历史 `snapd.seeded` 延迟；只有测量证明有收益时才优化，不把单次旧数据当成当前结论。

以上是种子清单，不是发现范围的上限；不要为凑任务数量编造问题。

## 4. 错误信息和输出的验收标准

每个关键错误应回答：哪里失败、实际情况与预期有什么差异、哪些节点或功能受影响、用户下一步具体做什么。

- 优先展示根因，合并同根因产生的重复症状。不能只打印原始系统输出，或笼统要求“检查配置”“重新安装”。
- 给出一条优先推荐、可直接执行且保留上下文的下一步；不能指向会原样返回同一错误的命令。
- 默认输出简短；耗时操作说明阶段和进展；完整细节放在 verbose 或诊断日志中。已自动恢复的过程通常不应再以失败结尾。
- 文本、JSON/YAML、退出码和最终状态一致。脚本输出保持可解析，新增字段兼容已有消费者。
- 可用但有限制、部分成功、尚未验证和真正失败必须区分；辅助集成失败不能抹掉已经成功的 VM 操作。

## 5. 任务排序与实施方式

先产出任务表，每项至少包含：优先级、用户场景、证据状态、复现方法、根因、资源归属、最小改动、用户可见的新行为、验收方法、预计规模和必要依赖。

证据状态分为“当前已复现”“代码已确认”“历史记录待复现”“待验证假设”。P0 是阻断核心路径、错误操作或误报成功；P1 是常见且可恢复的失败、升级阻碍和无效报错；P2 是低频问题、性能优化或需要较大设计变更的方向。

优先处理影响大、证据足、改动小的任务。边界明确的兼容修复直接完成，不停在报告上等待批准。较大设计选择给出推荐方案、取舍和最小可交付范围；继续其他独立任务。

保持 Farrow 的单部署模型，复用现有命令和恢复机制，不引入单独的 repair 命令。只做本次修复所需的代码整理；新网络模式、完整 UID 映射、命令语法破坏性调整等，评估后单列，避免拖大 0.8。

## 6. 验证与交付

为实质缺陷补能复现原问题的回归测试，再验证用户可见的修复和恢复边界。使用隔离配置、数据目录、VM 和专用测试主机注入故障；不要为了验证而重启当前工作机或损坏用户正在使用的部署。

运行与改动相关的检查，最终通过项目现有 `make check`。在可用环境中补 macOS arm64、Linux amd64 的真实安装到首次 SSH、旧 VM 升级和宿主重启后恢复验证；涉及 Pigsty 使用路径时验证一次实际连接或配置操作。缺少环境时明确标记未验证，继续完成可做的工作。

最后交付：

1. 按优先级排列的任务清单，明确已完成、待复现、后续版本和需要外部条件的项目。
2. 已完成修复的前后行为、代码位置、回归测试与实际执行证据。
3. 精简的 0.8 建议范围、必须满足的验收条件和剩余问题。
4. 对应的 CHANGELOG、必要帮助与文档更新。

明确区分源码检查、构建、真实 VM 验证、提交、安装和发布。把工作做到可审核、可使用；推送、打 tag 和发布按已有授权执行，没有授权时交付工作区结果。
