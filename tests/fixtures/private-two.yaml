---
# Two fixed-IP nodes as a Pigsty-compatible inventory.
all:
  vars:
    admin_ip: 10.10.10.10
  children:
    nodes:
      hosts:
        10.10.10.10: { nodename: meta, vm_cpu: 2, vm_mem: 4096 }
        10.10.10.11: { nodename: node-1 }
