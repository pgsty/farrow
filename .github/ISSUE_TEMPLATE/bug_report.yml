name: Bug report
description: Something Farrow does is wrong, and you can show it.
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        Do not report a suspected vulnerability here — follow
        [SECURITY.md](https://github.com/pgsty/farrow/blob/main/SECURITY.md).
  - type: textarea
    id: what-happened
    attributes:
      label: What happened
      description: What you ran, what you expected, and what you got instead.
      placeholder: |
        $ farrow up
        error: ...
    validations:
      required: true
  - type: textarea
    id: version
    attributes:
      label: Version and host
      description: Output of `farrow --json version`.
      render: json
    validations:
      required: true
  - type: textarea
    id: doctor
    attributes:
      label: farrow doctor
      description: Output of `farrow doctor`. This usually identifies the cause on its own.
      render: shell
    validations:
      required: true
  - type: textarea
    id: inventory
    attributes:
      label: Inventory
      description: The smallest `farrow.yml` that reproduces this. Remove anything sensitive.
      render: yaml
  - type: textarea
    id: verbose
    attributes:
      label: Verbose output
      description: Re-run the failing command with `--verbose` and paste the result.
      render: shell
